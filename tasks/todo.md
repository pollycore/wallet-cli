# Task Plan

- [x] Review the written instructions, docs, and current `pw test` placeholder behavior
- [ ] Add `"<PublicKey>"` fixture resolution for outbound `pw test` payloads using the wallet public key in `~/.pollyweb/public.pem`
- [ ] Add regression coverage for public-key placeholder expansion and the missing-key error path
- [ ] Update docs and repo guidance for the new `pw test` placeholder
- [ ] Run targeted verification and capture the review

# Review

- Pending implementation and verification for the `pw test` `"<PublicKey>"` placeholder update.
